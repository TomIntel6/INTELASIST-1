// Archivo que expone la versión actual de la aplicación.
// Actualiza este valor en cada despliegue/release para que el cliente
// detecte que hay una nueva versión y muestre el modal de actualización.
export const APP_VERSION = '0.0.1'

// Nota: se puede automatizar la sincronización con package.json en el
// pipeline de build (por ejemplo exportando VITE_APP_VERSION), pero para
// simplicidad aquí fijamos el valor manualmente.
